[metadata]
name = "bun"
description = "Fast all-in-one JavaScript runtime"
homepage = "https://bun.sh/"
version_format = "semver"
supported_os = ["linux"]

[version]
tag_prefix = "bun-v"

[[steps]]
action = "github_archive"
repo = "oven-sh/bun"
asset_pattern = "bun-{os}-{arch}.zip"
strip_dirs = 1
binaries = ["bun"]
os_mapping = { linux = "linux" }
arch_mapping = { amd64 = "x64", arm64 = "aarch64" }

[verify]
command = "bun --version"
pattern = "{version}"
version_format = "semver"
