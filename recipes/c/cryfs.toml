[metadata]
  name = "cryfs"
  description = "Encrypts your files so you can safely store them in Dropbox, iCloud, etc."
  homepage = "https://www.cryfs.org"
  version_format = ""
  requires_sudo = false
  tier = 0
  type = ""
  llm_validation = "skipped"
  runtime_dependencies = ["fmt"]
  # macOS: no homebrew bottle for sonoma
  unsupported_platforms = ["darwin/arm64", "darwin/amd64"]

[version]
  source = ""
  github_repo = ""
  tag_prefix = ""
  module = ""
  formula = ""
  cask = ""
  tap = ""
  fossil_repo = ""
  project_name = ""
  version_separator = ""
  timeline_tag = ""

[[steps]]
  action = "homebrew"
  formula = "cryfs"

[[steps]]
  action = "install_binaries"
  binaries = ["bin/cryfs", "bin/cryfs-unmount"]

[verify]
  command = "cryfs --version"
  pattern = ""
