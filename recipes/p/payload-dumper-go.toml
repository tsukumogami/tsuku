[metadata]
  name = "payload-dumper-go"
  description = "Android OTA payload dumper written in Go"
  homepage = "https://github.com/ssut/payload-dumper-go"
  version_format = ""
  requires_sudo = false
  runtime_dependencies = ["xz"]
  tier = 0
  type = ""
  llm_validation = "skipped"

[version]
  source = "homebrew"
  github_repo = ""
  tag_prefix = ""
  module = ""
  formula = "payload-dumper-go"
  cask = ""
  tap = ""
  fossil_repo = ""
  project_name = ""
  version_separator = ""
  timeline_tag = ""

[[steps]]
  action = "homebrew"
  formula = "payload-dumper-go"

[[steps]]
  action = "install_binaries"
  binaries = ["bin/payload-dumper-go"]

[verify]
  command = "payload-dumper-go --version"
  pattern = ""
