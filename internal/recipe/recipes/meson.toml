[metadata]
name = "meson"
description = "Fast and user-friendly build system"
homepage = "https://mesonbuild.com"

[[steps]]
action = "pipx_install"
package = "meson"
executables = ["meson"]

[verify]
command = "meson --version"
pattern = "{version}"
