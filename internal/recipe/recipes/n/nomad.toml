[metadata]
name = "nomad"
description = "Workload orchestrator"
homepage = "https://www.nomadproject.io/"
version_format = "semver"

[version]
github_repo = "hashicorp/nomad"

[[steps]]
action = "download"
url = "https://releases.hashicorp.com/nomad/{version}/nomad_{version}_{os}_{arch}.zip"
checksum_url = "https://releases.hashicorp.com/nomad/{version}/nomad_{version}_SHA256SUMS"

[[steps]]
action = "extract"
archive = "nomad_{version}_{os}_{arch}.zip"
format = "zip"

[[steps]]
action = "chmod"
files = ["nomad"]

[[steps]]
action = "install_binaries"
binaries = ["nomad"]

[verify]
command = "nomad version"
pattern = "{version}"
