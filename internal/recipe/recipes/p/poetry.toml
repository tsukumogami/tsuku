[metadata]
name = "poetry"
description = "Python dependency management and packaging made easy"
homepage = "https://python-poetry.org/"
version_format = "semver"

[[steps]]
action = "pipx_install"
package = "poetry"
executables = ["poetry"]

[verify]
command = "poetry --version"
pattern = "Poetry (version {version})"
