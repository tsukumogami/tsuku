# PR Split Plan: `docs/system-lib-backfill`

Updated: 2026-03-01

## Summary

Split 1,512 new recipes into 50 PRs using a mixed format: each PR
contains homebrew libraries, tools that test them, and cargo or gem
recipes. This replaces the earlier plan that grouped recipes by type.

The full per-recipe assignment is in `wip/pr-index.md`, generated by
`scripts/generate-pr-index.py`.

### PR structure

| Phase | PRs | Per-PR contents | Total recipes |
|-------|-----|-----------------|---------------|
| Mixed (cargo) | 14 (PRs 2-15) | 5 libs + 5 tools + 6 cargo | ~224 |
| Mixed (gem) | 14 (PRs 16-29) | 5 libs + 5 tools + 6 gem | ~224 |
| Overflow | 22 (PRs 30-51) | ~50 homebrew tools | ~1,064 |
| **Total** | **50** | | **1,512** |

### Design rationale

- Rust recipes are slow (~20 min per batch of 3). Capping at 6 per PR
  keeps CI times manageable.
- Libraries land alongside tools that depend on them, so CI validates
  the libraries actually work at runtime.
- High-fan-out libraries (glib, libusb, libevent) land in the first
  few PRs to unblock their dependents.
- Overflow batches are ordered by priority queue ranking.

## Completed PRs

### PR 1: `backfill/pr1-infra` (PR #1957) -- MERGED

Merged 2026-03-01. 48 files (3 infra + 45 modified recipes).

Contents:
- `.github/ci-batch-config.json` -- per-ecosystem batch sizes
- `.github/workflows/test-recipe.yml` -- ecosystem sorting, batching,
  per-recipe timing, platform pre-filtering
- `data/queues/priority-queue.json` -- batch generation ordering
- 45 modified recipes: satisfies sections, crates_io source, runtime_deps

## Remaining PRs

See `wip/pr-index.md` for the full per-recipe assignment.

Key PRs in the critical path:
- **PR 2**: glib, libevent, libusb, libomp, libogg + 5 tools + 6 cargo
- **PR 3**: mpfr, libidn2, graphene, notmuch, libgee + 5 tools + 6 cargo
- **PR 4**: liblqr, libslirp, gupnp-av, jsonrpc-glib, libdex + 5 tools + 6 cargo
- **PR 5**: libgit2-glib, unbound, qtsvg, jansson, libsamplerate + 5 tools + 6 cargo

## Deferred recipes

These recipes are excluded until their blocking issues are resolved:

| Recipe | Issue | Reason |
|--------|-------|--------|
| git | #1964 | Sandbox dep resolution failure with runtime_dependencies |
| spatialite | #1964 | Sandbox dep resolution failure with runtime_dependencies |
| fontconfig | #1965 | macOS rpath bug for homebrew library deps |
| proj | #1965 | macOS rpath bug for homebrew library deps |
| cuda-runtime | -- | System package, not a homebrew formula |
| mesa-vulkan-drivers | -- | Pre-existing Alpine arm64 bug |
| vulkan-loader | -- | System package, not a homebrew formula |

## Issues filed

| Issue | Description | Status |
|-------|-------------|--------|
| #1964 | Circular dependency detection bug | Open |
| #1965 | macOS homebrew recipes missing rpath for runtime dep libraries | Open |
| #1970 | Dashboard: invalid queue status "generated" | Open |
