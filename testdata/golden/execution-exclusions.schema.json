{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Execution Validation Exclusions",
  "description": "Recipes excluded from golden file execution validation (validate-linux and validate-macos jobs in validate-golden-execution.yml). These recipes have golden files but fail during actual installation due to toolchain issues.",
  "type": "object",
  "required": ["exclusions"],
  "properties": {
    "description": {
      "type": "string"
    },
    "exclusions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["recipe", "issue", "reason"],
        "properties": {
          "recipe": {
            "type": "string",
            "description": "Recipe name (e.g., 'amplify', 'cdk')"
          },
          "issue": {
            "type": "string",
            "format": "uri",
            "pattern": "^https://github.com/.+/issues/[0-9]+$",
            "description": "GitHub issue URL tracking the blocker"
          },
          "reason": {
            "type": "string",
            "description": "Brief explanation of why execution validation is skipped (e.g., toolchain version drift)"
          }
        }
      }
    }
  }
}
